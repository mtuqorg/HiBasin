1. The observed data are stored in the list of obspy stream. Each element is for one staton including 1 or 2 or 3 components,even empty traces when the data is discarded.
1. The Green's functions are stored in the list of obspy stream. Similarly, each element is for one station including 10 GFs.  
   Importantly, mtuq uses a up-south-east basis convention.
